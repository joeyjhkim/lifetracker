# Dependencies (40k+ files — never commit)
/node_modules

# Build artifacts (regenerated from source)
/build
/dist

# User data exports (never accidentally commit if user runs export into repo dir)
lifetracker-*.json
*.backup-*.json
lifetracker.json
window-state.json

# Environment / secrets (forward-defense — none today)
.env
.env.local
.env.*.local
*.pem
*.key

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.AppleDouble
Thumbs.db

# Claude Code per-developer config (contains absolute paths)
.claude/settings.local.json

# Signing artifacts
*.mobileprovision
*.p12
